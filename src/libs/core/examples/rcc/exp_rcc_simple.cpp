// #include <utk_resource.h>
#include <octk_logging.hpp>

#include <stdio.h>


int main()
{
    // utk_logging_category_switch_level(UTK_DEFAULT_CATEGORY(), UTK_LOGGING_LEVEL_VERBOSE);
    // UTK_INIT_RESOURCE(utk_exp_rcc_simple);
    // fprintf(stderr, "%s example!\n", UTK_PP_STRINGIFY(EXECUTABLE_NAME));
    return 0;
}
